# CLAUDE.md

이 파일은 이 저장소에서 작업하는 Claude Code(및 다른 AI 코딩 어시스턴트)를 위한 프로젝트 가이드다.

## 프로젝트 개요

개인 투자자(미국 나스닥·S&P500 종목 보유)를 위한 **중장기 추세 이탈 조기 감지 알림 시스템**을 만든다.

보유 종목/관심종목을 매일 자동으로 스캔하여, 주~월 단위로 서서히 진행되는 하락 추세를 조기에 감지하고 "유지관찰 / 비중축소 / 매도" 중 하나의 행동 제안과 함께 알림을 보낸다. 데이트레이딩용 당일 종가 예측 모델이 아니다 — 이 부분은 프로젝트 초기에 검토했다가 실제 문제(장기 보유 종목의 완만한 하락을 못 알아채는 문제)와 맞지 않아 폐기했다.

**전체 요구사항은 [`개발 요구 사항 문서.md`](./개발%20요구%20사항%20문서.md)를 단일 진실 공급원(source of truth)으로 삼는다.** 기능을 구현하거나 설계를 바꾸기 전에 반드시 이 문서를 먼저 확인할 것.

세부 운영 규칙(백로그 변경 주체, 상태 전이, 코드 책임 분리, 문서 갱신 조건 등)은 `.claude/rules/backlog-governance.md`에 있으며, 세션 시작 시 이 CLAUDE.md와 함께 자동 로드된다(`/context`로 로드 여부 확인 가능).

## 반드시 지켜야 할 핵심 제약 (요구사항 문서에서 확정됨)

- **데이터 소스는 무료만 사용 가능**: 무료 가격/거래량 데이터(Yahoo Finance류) + 무료 뉴스(RSS 등). 유료 펀더멘털/애널리스트 데이터, 증권사 API 연동은 사용하지 않는다. 이 제약을 벗어나는 라이브러리·API를 도입하기 전에 사용자에게 먼저 확인한다.
- **모니터링 범위는 소규모**: 보유 종목 + 관심종목 합쳐서 10개 미만. 대규모 스크리닝/유니버스 스캔 기능으로 과설계하지 않는다.
- **스캔은 매일 1회, 알림은 이상 감지 시에만**: 상시 대시보드보다는 "조용하다가 문제 있을 때만 알림 오는" 방식이 목표다.
- **등급별로 판단 기준의 엄격도가 다르다** — 이것이 이 시스템의 핵심 설계 원칙이다:
  - `유지관찰`: 가격 또는 뉴스 신호 중 하나만 있어도 낮은 문턱에서 발동 (recall 우선, 민감하게)
  - `비중축소`: 유지관찰보다 강하거나 복수 신호가 부분적으로 결합될 때 발동
  - `매도`: 가격 하락 + 부정적 뉴스 등 복수 신호가 동시에 확인될 때만 발동 (precision 우선, 신중하게)
  - 이유: 사용자는 "매도" 알림을 받으면 근거를 보고 거의 그대로 실제 매도를 실행한다. 따라서 "매도" 등급의 오탐은 실제 금전적 손실로 이어진다. 반대로 시스템 전체의 원칙은 "놓치는 것(false negative)이 오탐보다 나쁘다"이므로, 이 recall 우선 원칙은 `유지관찰` 등급에만 느슨하게 적용하고 `매도` 등급까지 확대 적용하면 안 된다.
- **가격 하락 판단은 절대 하락률 기준**이다 (벤치마크 대비 상대 저조 단독 기준이 아님). 계기 사례(코스트코/펩시코)가 시장·섹터 전체와 함께 하락했기 때문에, 벤치마크 대비 상대 성과만으로는 걸러지지 않는다는 점이 이미 검증되었다. 다만 알림에는 "시장 전체 조정인지 종목 고유 문제인지" 맥락을 함께 표시해야 한다.
- **뉴스는 가격 확인 후 사후 설명용이 아니라 독립적인 선행 트리거**다. 가격이 아직 안 빠졌어도 부정적 뉴스(실적/가이던스, 규제/소송/정치적 리스크)만으로 조기경보가 울릴 수 있어야 한다.
- **재진입(매수) 타이밍은 이번 범위에서 제외**한다. 하락/재고 경고(매도 방향)에만 집중한다.
- **당일 종가 예측 기능은 만들지 않는다.** 관련 요청이 다시 나오면 요구사항 문서의 "범위 제외 사항"을 먼저 참고해 사용자에게 의도를 확인한다.

## 기술 스택 결정 (P0.1, 2026-09-15 사용자 승인)

- **언어/실행 환경**: Python 스크립트, 로컬(Windows) 실행.
- **스케줄러**: Windows 작업 스케줄러(Task Scheduler)로 매일 1회 실행 트리거.
- **알림 발송**: 카카오톡 "나에게 보내기(Talk Memo)" API를 기본 채널로 사용한다. 카카오 디벨로퍼스 앱 등록과 최초 1회 본인 카카오 계정 OAuth 로그인(액세스·리프레시 토큰 발급)은 보안상 사용자가 직접 수행해야 하며, 이 세션이 대신 로그인할 수 없다. 리프레시 토큰은 약 2개월 후 만료되어 재로그인이 필요할 수 있다. 카카오 발송이 실패(토큰 만료 등)하면 이메일(SMTP)을 백업 채널로 자동 전환한다.
- **가격/거래량 데이터**: `yfinance`(비공식 Yahoo Finance 라이브러리).
- **뉴스 데이터**: Google News RSS(종목명/티커 기반 검색 피드).
- 승인 근거: 2026-09-15 사용자가 대화에서 위 조합을 직접 확정함 (P0.1 `done_when` 충족 근거).
- **데이터 소스 실호출 검증(P0.3, 2026-09-15)**: `yfinance.download("AAPL", period="5d")`로 실제 가격/거래량 5행 수신 확인. `feedparser.parse("https://news.google.com/rss/search?q=...")`로 실제 뉴스 항목 100건 수신 확인. 두 소스 모두 API 키 없이 무료로 접근 가능함을 확인함 (P0.3 `done_when` 충족 근거).

## 개발/검증 방침

- 실제 매매에 이 시스템의 판단을 반영하기 전에, **페이퍼 트레이딩(무자본 관찰) 2~3개월** 기간을 두고 검증한다. 성공 기준은 그 기간 중 실제 재고가 필요했던 하락 사례를 최소 1건 이상 놓치지 않고 잡아내는 것이다.
- 이 시스템은 최종 매매를 자동으로 실행하지 않는다. 알림 + 행동 제안까지가 범위이며, 실행은 사용자가 직접 한다. 자동매매 기능을 임의로 추가하지 않는다.

## 작업 실행 순서

새 작업에 착수할 때는 아래 순서를 따른다. 각 단계의 세부 규칙은 `.claude/rules/backlog-governance.md`를 따른다.

1. **기준 확인**: `개발 요구 사항 문서.md`로 요구사항을, `python3 scripts/backlog_cli.py list|get <id>|validate`로 백로그를, 이 CLAUDE.md와 `.claude/rules/`로 현재 규칙을 확인한다.
2. **작업 선택**: `python3 scripts/backlog_cli.py ready`로 `deps`가 모두 `done`인 착수 후보를 확인하고, 해당 작업의 `gate`·`done_when`을 먼저 읽어 완료 판정 기준을 파악한 뒤 작은 단위의 작업을 고른다.
3. **착수 기록**: `set-status --status in_progress --owner <실제 담당>`으로 착수 상태·담당·시간을 실제 사실에 맞게 기록한다.
4. **구현 + 검사**: `scripts/`·`.claude/hooks/`(파일당 300줄, ruff, py_compile)와 프로젝트 루트의 단일 HTML(800줄, 금지 패턴 검사, html.parser 파싱)에 대한 코드 구조 규칙을 지키며 구현한다. `PostToolUse` 훅(`check_backuplog.py`, `quick_quality_check.py`)이 저장 시 자동 검사한다.
5. **병렬 검토 준비**: `validate`로 `file_sha256`/`meta_updated`를 고정한 뒤, 동일한 스냅샷을 `critical-reviewer`와 `backlog-explainer` 서브에이전트에 똑같이 전달해 병렬 실행한다.
6. **반영**: 두 결과를 종합하고, 요구사항 문서·코드로 근거가 확인된 지적만 `update`/`set-status`/`add`로 반영한 뒤 `validate`나 격리 사본으로 필요한 재검증을 수행한다.
7. **완료 기록**: `done_when` 충족, 검사 통과, 리뷰 반영이 모두 확인된 경우에만 `set-status --status done --note <근거>`로 기록한다. 리뷰 미실행·검사 실패·미설정(NOT_CONFIGURED 포함) 검사는 완료 근거로 쓰지 않는다.
8. **연동 확인**: JSON 변경 후 영향받은 `docs/backlog/<id>.md`(해당 task의 `doc` 필드)와 `backlog-dashboard.html`에서 최신 상태를 확인한다.
9. **중단 대비 기록**: 상태가 바뀔 때마다 그 즉시 `set-status`로 `backuplog.json`에 반영한다. 세션이 언제 중단되어도 다음 세션이 현재 상태 그대로 이어받을 수 있어야 한다.

## 작업 관리 (backuplog.json)

이 프로젝트의 작업 항목(우선순위, 진행 순서, 상태)은 [`backuplog.json`](./backuplog.json)에서 관리한다. **직접 Read/Grep/Glob/`cat`으로 열거나 고치지 않는다** — `.claude/hooks/guard_backlog_read.py`가 이를 차단하며, 조회·변경은 반드시 아래 CLI를 거친다.

- 조회(읽기 전용): `python3 scripts/backlog_cli.py list|get <id>|ready|validate`
- 변경: `python3 scripts/backlog_cli.py add|update|set-status` — 저장 전 스키마·참조·순환 의존성 검증, 저장 전 `.backlog-backups/` 백업, `--expect-hash`로 동시 변경 충돌 감지를 자동 수행하며, 검증 실패 시 원본을 그대로 보존한다.
- 상태 전이 조건, 담당·시간 기록 원칙, 완료(done) 판정 기준, 코드 책임 분리, 문서 갱신 시점·경로, 동시 실행 시 입력 버전 고정 규칙은 `.claude/rules/backlog-governance.md`에 상세히 정리되어 있다.

## 문서 갱신 규칙

- 요구사항이 바뀌면 먼저 사용자와 논의하여 `개발 요구 사항 문서.md`를 갱신하고, 그 다음에 이 `CLAUDE.md`의 관련 항목을 함께 갱신한다. 코드만 먼저 바꾸고 문서를 방치하지 않는다.
- 기술 스택은 위 "기술 스택 결정" 절에서 사용자 승인과 함께 확정되었다. 이후 스택을 변경(언어/프레임워크/배포 방식/알림 채널 등)하려면 임의로 바꾸지 말고 사용자에게 먼저 확인한 뒤 해당 절을 갱신한다.
- 이 절은 `개발 요구 사항 문서.md`/`CLAUDE.md` 자체의 갱신 규칙이다. 작업별 설명 문서(`docs/backlog/<id>.md`)의 갱신 시점·경로·주체는 `.claude/rules/backlog-governance.md` §4를 따른다.
